# AI-File-Assistant

A Python-based smart file tracker with semantic search.

## Features
- Real-time file indexing (PDFs, text, code)
- Semantic search powered by `sentence-transformers`
- Gradio UI for natural language queries

## Setup
1. Clone the repo:
   ```bash
   git clone https://github.com/lesmath/AI-File-Assistant.git
   cd AI-File-Assistant
